# WordPress Adonis Theme
A simple theme for wordpress development with modern workflow

## Installation

Install all the dependencies by running:
```bash
$ composer install
```

Install all the packages by running:
```bash
$ npm install
```